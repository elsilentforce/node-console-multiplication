# Node Console Multiplication App #
This application uses the console to generate multiplying tables into plain text files.

## How to Use ##
Just run the command at your console
```
npx ts-node src/app.ts --base <number>
```
